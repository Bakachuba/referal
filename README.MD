# 1) Create PostgreSql DB with parametrs:

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'referal',
        'USER': 'USER',
        'PASSWORD': '2585',
        'HOST': '',
        'PORT': '5432',
    }
}